//get the value of an array index. based on that value it pastes it into a new array.
// repeat for all the indexes.
// remove the blank/undefined indexes of the new array and then returns the new array as a sorted list
